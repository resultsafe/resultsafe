✅ Notes

allSettled(tasks) запускает все задачи параллельно.

Возвращает TaskResult<Result<T, E>[], never> — всегда Ok с массивом Result.

Можно безопасно обработать каждую задачу, независимо от ошибок.

Rust-подобный стиль: никаких исключений, только Ok/Err.
