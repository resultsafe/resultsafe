{
  "name": "resultsafe-docs-portal",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "collect": "node scripts/collect-site.js",
    "lint:i18n": "node scripts/lint-i18n.js",
    "start": "npm run collect && docusaurus start",
    "build": "npm run collect && npm run lint:i18n && docusaurus build",
    "serve": "docusaurus serve",
    "clear": "docusaurus clear"
  },
  "dependencies": {
    "@docusaurus/core": "^3.5.2",
    "@docusaurus/preset-classic": "^3.5.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "fs-extra": "11.0.0",
    "glob": "10.0.0"
  }
}
